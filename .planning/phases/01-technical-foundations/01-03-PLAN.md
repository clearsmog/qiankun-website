---
phase: 01-technical-foundations
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - docs/.vitepress/config.js
autonomous: false
requirements: [INFRA-09]
user_setup:
  - service: cloudflare-web-analytics
    why: "Cookieless analytics is enabled by a dashboard toggle that auto-injects the beacon at the edge. There is deliberately no repo-side beacon snippet, so this cannot be done by the executor."
    dashboard_config:
      - task: "Enable Web Analytics on the Pages project"
        location: "Cloudflare dashboard -> Workers & Pages -> qiankun-website -> Metrics -> Web Analytics -> Enable"

must_haves:
  truths:
    - "`docs/.vitepress/config.js` `head` array contains no Google Analytics script entry and no measurement ID"
    - "built output under `docs/.vitepress/dist/` contains no Google Tag Manager reference on any page"
    - "the `head` array's non-analytics entries (favicon, RSS alternate, author/keywords meta, Open Graph and Twitter card meta) are all still present"
    - statement: Cloudflare Web Analytics is enabled on the qiankun-website Pages project and the injected beacon script is present in live page source at qiankun.co.uk
      verification: backstop
  artifacts:
    - "No new files — the beacon is injected at the edge by Cloudflare, deliberately not committed to the repo"
  key_links:
    - "Enabling Cloudflare Web Analytics must precede the GA-removal commit. Cloudflare injects the beacon on the next deployment, so the deploy carrying GA removal becomes the first deploy carrying the beacon — one clean handoff with no window where both run and no window where neither runs."
    - "Dashboard-level Transform Rules or a no-transform cache header would silently block edge injection. The toggle succeeding is not evidence the beacon shipped; only live page source is."
---

<!-- planner-discipline-allow: googletagmanager -->
<!-- planner-discipline-allow: G-4PF046MSJJ -->
<!-- planner-discipline-allow: gtag -->

<objective>
Swap Google Analytics for cookieless Cloudflare Web Analytics in a single deploy, with no period where both are running and no period where neither is.

Purpose: The current analytics sets cookies and ships visitor data to a third party with no consent surface. Replacing it with a cookieless equivalent removes the need for a consent banner rather than building one.
Output: Google Analytics gone from the VitePress config and from built output; Cloudflare Web Analytics enabled at the edge and confirmed present in live page source.
</objective>

<execution_context>
@/Users/qiankun/.claude/gsd-core/workflows/execute-plan.md
@/Users/qiankun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-technical-foundations/01-CONTEXT.md
@.planning/phases/01-technical-foundations/01-RESEARCH.md
@.planning/phases/01-technical-foundations/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

Modified by this plan:
- `docs/.vitepress/config.js` — the two Google Analytics `head` entries and their comment removed

Created by this plan: none. The Cloudflare beacon is injected at the edge on response and is deliberately never committed — that is the whole point of choosing dashboard auto-injection over a hand-maintained snippet, since a committed snippet can drift from the dashboard's actual state.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Human enables Cloudflare Web Analytics on the Pages project</name>
  <precondition>The user has dashboard access to the `qiankun-website` Cloudflare Pages project. There is no CLI path planned for this by design, so halt and report if dashboard access is unavailable.</precondition>
  <action>
    No file changes in this task. This checkpoint deliberately runs FIRST, before Google Analytics is removed, and the ordering is the point.

    Cloudflare injects its beacon on the next deployment of the Pages project. Enabling it now means the deploy that carries the GA-removal commit from Task 2 is also the first deploy carrying the new beacon: analytics hands over on a single deploy, with no window where both systems are running and no window where neither is. Removing GA first would leave a measurement gap; enabling after the beacon was already live would double-count.

    The action is dashboard-only by design — the locked decision is edge auto-injection rather than a committed beacon snippet, specifically so there is no repo-side token that can drift from the dashboard's real state. The executor therefore has nothing to run here.

    The proxy precondition for auto-injection is already satisfied and needs no separate check: plan 01-01's live DNS query returned Cloudflare anycast addresses, which is only possible for a proxied (orange-clouded) domain.

    Present the instructions below to the user verbatim and halt until they respond.
  </action>
  <instructions>
    Click-path quoted from Cloudflare's own current documentation:

    1. Log in to the Cloudflare dashboard.
    2. Navigate to **Workers & Pages**.
    3. Select the `qiankun-website` Pages project.
    4. Open the **Metrics** tab.
    5. Under **Web Analytics**, click **Enable**.

    No repository change is needed — Cloudflare adds the script to the HTML response itself, on the next deployment.

    Do not attempt to confirm the beacon is live yet. It will not appear until the next deployment, which is the commit produced by Task 2. Task 3 is where the beacon gets verified.

    Reversal, if wanted: disable the same toggle. No repo change is involved either way.
  </instructions>
  <verification>
    Confirmation is the user's report that Web Analytics shows as enabled on the `qiankun-website` Pages project's Metrics tab. No repository-side evidence exists for this, and none will until the beacon reaches live page source in Task 3 — this contributes to a `verification: backstop` truth, so an unconfirmed or unrecorded toggle must not be treated as satisfied.
  </verification>
  <acceptance_criteria>
    - The user explicitly confirms Web Analytics shows as enabled on the `qiankun-website` Pages project's Metrics tab
    - The confirmation is recorded in the SUMMARY (this contributes to a `verification: backstop` truth — no repository evidence for it exists, so an unrecorded or unconfirmed toggle must not be treated as satisfied)
  </acceptance_criteria>
  <resume-signal>Type "approved" once Web Analytics is enabled, or describe what the dashboard showed instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove Google Analytics from the VitePress config</name>
  <files>docs/.vitepress/config.js</files>
  <read_first>
    - `docs/.vitepress/config.js` lines 94-143 — the whole `head` array, so the entries that must survive are known before deleting anything
    - `docs/.vitepress/config.js` lines 130-142 specifically — the `// Google Analytics` comment and the two array entries beneath it (the async `googletagmanager` script tag, and the inline `gtag` bootstrap carrying measurement ID `G-4PF046MSJJ`)
    - `.planning/phases/01-technical-foundations/01-PATTERNS.md` section `docs/.vitepress/config.js (unwrap withMermaid, remove GA)` — has the exact entries to delete and the exact boundary of what to keep
  </read_first>
  <action>
    Delete the two Google Analytics entries from the `head` array along with the `// Google Analytics` comment that introduces them: the array entry declaring the async script sourced from `googletagmanager` with measurement ID `G-4PF046MSJJ`, and the array entry containing the inline `gtag` / `dataLayer` bootstrap for the same ID. Delete these outright rather than commenting them out.

    Everything above them in the `head` array must survive untouched: the favicon link, the RSS alternate link, the author meta, the keywords meta, the four Open Graph meta entries, and the two Twitter card meta entries. After the deletion the `twitter:image` meta entry becomes the array's last element — make sure the array's bracket and comma structure is still valid.

    Do not leave a comment recording what was removed. The acceptance gate greps this file and the built output for the removed identifiers, and an explanatory comment would fail a gate on otherwise-correct code.

    Change nothing else in this file. Plan 01-02 already unwrapped the config export and removed the mermaid key; do not revisit either.

    Then rebuild and check the built output, not just the source. The built HTML is the real evidence — it is what actually ships to visitors, and it contains no source comments, so it is the stronger assertion.
  </action>
  <verify>
    <automated>npm run docs:build &amp;&amp; test "$(grep -rl 'googletagmanager' docs/.vitepress/dist/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'googletagmanager|G-4PF046MSJJ|gtag' docs/.vitepress/config.js` returns `0`
    - `grep -c 'twitter:image' docs/.vitepress/config.js` returns at least `1` (surviving head entries intact)
    - `grep -c 'application/rss+xml' docs/.vitepress/config.js` returns at least `1`
    - `grep -c 'og:image' docs/.vitepress/config.js` returns at least `1`
    - `npm run docs:build` exits 0
    - `grep -rlE 'googletagmanager|G-4PF046MSJJ' docs/.vitepress/dist/ | wc -l` returns `0`
    - `grep -rl 'og:image' docs/.vitepress/dist/index.html | wc -l` returns `1` (the surviving meta actually renders)
  </acceptance_criteria>
  <reversibility rating="reversible">`git revert` restores both head entries; the Cloudflare toggle is independently reversible from the dashboard.</reversibility>
  <done>Google Analytics is absent from the config source and from every page of built output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the beacon actually shipped and Google Analytics is gone from the live site</name>
  <what-built>
    Task 1 enabled Cloudflare Web Analytics on the Pages project. Task 2 removed Google Analytics from the config and proved it absent from local build output. Once the Task 2 commit is pushed and the GitHub Actions workflow finishes deploying, the live site should be serving the Cloudflare beacon and nothing from Google.

    This check exists because the dashboard toggle succeeding is not evidence the beacon shipped. Cloudflare's edge injection is silently blocked by a `no-transform` cache directive or by a dashboard-level Transform Rule. Nothing in this repository sets either — there is no `_headers` file in `docs/public/` — but dashboard-only configuration is not visible from the repo and was never inspected. If injection is blocked, the site ends up with no analytics at all while plan 01-04 publishes a privacy page describing analytics that is not running.
  </what-built>
  <how-to-verify>
    Wait for the GitHub Actions deploy from Task 2's commit to complete, then:

    1. Open `https://qiankun.co.uk` and view page source (not the rendered DOM — view-source, so you see what the edge actually returned).
    2. Search the source for `beacon.min.js`. It should be present, referencing a Cloudflare Insights script with a token. That is the injected beacon.
    3. Search the same source for `googletagmanager` and for `G-4PF046MSJJ`. Both should return nothing.
    4. Repeat step 1-3 on one case-study page, for example `https://qiankun.co.uk/projects/uk-finance-pay`, to confirm injection applies site-wide rather than only to the homepage.

    If the beacon is absent: check the Cloudflare dashboard for Transform Rules or Page Rules on this zone that set a `no-transform` cache directive, and report back rather than working around it — a repo-side beacon snippet is explicitly not the chosen approach.
  </how-to-verify>
  <acceptance_criteria>
    - The user confirms `beacon.min.js` appears in the live page source of `https://qiankun.co.uk`
    - The user confirms `beacon.min.js` also appears on a `/projects/` case-study page
    - The user confirms neither `googletagmanager` nor `G-4PF046MSJJ` appears in live page source on either page
    - The result is recorded in the SUMMARY as the INFRA-09 backstop evidence. If the beacon cannot be confirmed present, this truth must be reported as unmet rather than assumed — a passing dashboard toggle is not a substitute.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the beacon is present on both pages and no Google Analytics reference remains, or paste what you found instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser -> third-party analytics | Analytics scripts run in the visitor's browser and transmit request metadata off-site. |
| Cloudflare edge -> HTML response body | The beacon is injected into the response at the edge, outside the repository's control or review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Information Disclosure | Google Analytics setting cookies and transmitting visitor data to a third party with no consent surface on a UK-audience site | medium | mitigate | Task 2 removes it entirely; the replacement is cookieless and stores no persistent identifier, which removes the need for a consent banner rather than building one |
| T-01-09 | Information Disclosure | A window where both analytics systems run simultaneously, double-collecting visitor data | low | mitigate | Task 1 is ordered before Task 2 so the single deploy carrying GA removal is also the first deploy carrying the beacon — the handoff is atomic |
| T-01-10 | Repudiation | Edge injection silently blocked by a dashboard Transform Rule or `no-transform` header, leaving the site with no analytics while plan 01-04 publishes a privacy page claiming analytics is in use | medium | mitigate | Task 3 verifies the beacon in live page source on two different page types rather than trusting the dashboard toggle; the failure mode and its likely cause are named in the checkpoint |
| T-01-11 | Tampering | Edge-injected third-party script in the response body is not reviewable in the repository | low | accept | This is the locked decision and the deliberate trade: a committed snippet can silently drift from the dashboard's real state, and the injecting party is the same party already terminating TLS and serving every byte of the site |
</threat_model>

<verification>
- Config source contains no Google Analytics identifier of any form
- Built output under `docs/.vitepress/dist/` contains no Google Tag Manager reference on any page
- Surviving `head` entries (favicon, RSS, author, keywords, Open Graph, Twitter card) still render into built output
- Live page source on both the homepage and a case-study page carries the Cloudflare beacon and no Google reference (backstop — human-confirmed, recorded in the SUMMARY)
</verification>

<success_criteria>
INFRA-09 is satisfied: Google Analytics is removed and cookieless Cloudflare Web Analytics is confirmed active in its place on the live site, with no overlap and no gap between the two.
</success_criteria>

<output>
Create `.planning/phases/01-technical-foundations/01-03-SUMMARY.md` when done. Record both human confirmations (dashboard enable, live beacon verification) as the INFRA-09 backstop evidence.
</output>
