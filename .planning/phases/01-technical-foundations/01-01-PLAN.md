---
phase: 01-technical-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - amplify.yml
  - deploy.sh
  - package.json
  - README.md
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03]
user_setup:
  - service: aws-amplify
    why: "Amplify auto-build on `main` must be disabled before the repo's Amplify files are deleted. No AWS CLI is configured on this machine, so this is console-only."
    dashboard_config:
      - task: "Disable auto build on the `main` branch"
        location: "AWS Amplify console -> select app -> App settings -> Branch settings -> Branches table -> `main` row -> Actions -> Disable auto build"

must_haves:
  truths:
    - "`dig +short qiankun.co.uk A` returns only Cloudflare-range addresses and no `amplifyapp.com` endpoint appears anywhere in the DNS answer"
    - statement: AWS Amplify auto-build on the `main` branch is disabled in the AWS console and the live site was observed stable before any repo file was deleted
      verification: backstop
    - "`amplify.yml` and `deploy.sh` do not exist in the repository"
    - "`package.json` `scripts` contains no `deploy` and no `deploy:quick` entry"
    - "`README.md` Deploy section describes the GitHub Actions to Cloudflare Pages path and contains no reference to AWS Amplify"
  artifacts:
    - ".planning/phases/01-technical-foundations/01-01-SUMMARY.md recording the live `dig` output and the documented serving pipeline (INFRA-01 evidence)"
  key_links:
    - "The Amplify-disable checkpoint gates the deletion task — Task 3 must not run until the human confirms Task 2. Deleting first destroys the emergency fallback."
    - "`deploy.sh` deletion and the `package.json` script removal must land in the same commit, or `npm run deploy` becomes a broken entry point."
---

<objective>
Establish that Cloudflare Pages via GitHub Actions is the only path that can deploy qiankun.co.uk, without ever leaving the live site without a fallback.

Purpose: Two independent pipelines can currently publish to a domain printed on a CV. This plan reduces that to one, in an order where every step is individually reversible.
Output: DNS serving path documented from a live observation; Amplify auto-build disabled in the console; `amplify.yml`, `deploy.sh`, and the two `package.json` deploy scripts removed; `README.md` Deploy section corrected.
</objective>

<execution_context>
@/Users/qiankun/.claude/gsd-core/workflows/execute-plan.md
@/Users/qiankun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-technical-foundations/01-CONTEXT.md
@.planning/phases/01-technical-foundations/01-RESEARCH.md
</context>

## Artifacts this phase produces

Created by this plan:
- `.planning/phases/01-technical-foundations/01-01-SUMMARY.md` — carries the INFRA-01 DNS finding as the phase's documented evidence

Deleted by this plan:
- `amplify.yml`
- `deploy.sh`
- `package.json` `scripts.deploy` and `scripts.deploy:quick`

Created elsewhere in phase 01 (for reference, not this plan):
- `docs/privacy.md` (plan 01-04)
- `docs/.vitepress/theme/PrivacyFooterLink.vue` (plan 01-04)
- `layout-bottom` slot key in `docs/.vitepress/theme/index.js` (plan 01-04)

<tasks>

<task type="tracer">
  <name>Task 1: Verify the live serving pipeline by direct DNS inspection</name>
  <files>(read-only — no file modified; finding is recorded in the plan SUMMARY)</files>
  <read_first>
    - `.planning/phases/01-technical-foundations/01-CONTEXT.md` (the recorded 2026-07-25 `dig` result: `qiankun.co.uk` -> `104.21.0.223` / `172.67.128.87`, `www` -> same pair)
    - `.planning/phases/01-technical-foundations/01-RESEARCH.md` section `Cloudflare Web Analytics — Verified Click-Path` (explains why a Cloudflare-range answer also proves the domain is proxied, which the analytics plan depends on)
    - `.github/workflows/deploy.yml` (the pipeline being confirmed as the survivor)
  </read_first>
  <action>
    Re-run the DNS inspection live rather than trusting the recorded value, because every subsequent step in this phase is gated on it and the check costs seconds. Query the A records for both `qiankun.co.uk` and `www.qiankun.co.uk`, and query for CNAME records on both names. Confirm every returned address falls in Cloudflare's anycast ranges (the recorded observation was the `104.21.x` and `172.67.x` pairs) and that no AWS-hosted endpoint appears anywhere in the answer chain. This is a read-only observation — do not touch, add, or modify any DNS record; the domain is established and printed on published materials. Record the exact command output verbatim in the SUMMARY as the INFRA-01 evidence, alongside the one-line conclusion naming which pipeline actually serves the domain. If the answer does NOT resolve to Cloudflare ranges, halt the entire phase and report — that would invalidate the premise of Task 2 and Task 3.
  </action>
  <verify>
    <automated>dig +short qiankun.co.uk A | grep -Eq '^(104\.2[0-9]\.|172\.6[4-9]\.)' &amp;&amp; dig +short www.qiankun.co.uk A | grep -Eq '^(104\.2[0-9]\.|172\.6[4-9]\.)'</automated>
  </verify>
  <acceptance_criteria>
    - `dig +short qiankun.co.uk A` returns at least one address matching `^(104\.2[0-9]\.|172\.6[4-9]\.)`
    - `dig +short www.qiankun.co.uk A` returns at least one address matching `^(104\.2[0-9]\.|172\.6[4-9]\.)`
    - `dig +short qiankun.co.uk CNAME; dig +short www.qiankun.co.uk CNAME; dig +short qiankun.co.uk A` piped through `grep -c 'amplifyapp'` returns `0`
    - The SUMMARY contains the verbatim command output and names Cloudflare Pages via GitHub Actions as the observed serving pipeline
  </acceptance_criteria>
  <reversibility rating="reversible">Read-only DNS query; changes nothing.</reversibility>
  <done>The live serving pipeline is confirmed to be Cloudflare, evidenced by command output recorded in the SUMMARY (INFRA-01).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Human disables AWS Amplify auto-build and observes the site stable</name>
  <precondition>The user has sign-in access to the AWS Amplify console for the account hosting this app. No AWS CLI is configured on this machine, so there is no programmatic fallback — halt and report if console access is unavailable.</precondition>
  <action>
    Task 1 confirmed by live DNS query that Cloudflare — not Amplify — serves `qiankun.co.uk`. Amplify is therefore a redundant second pipeline that can still auto-build and publish on every push to `main`, and it must be switched off before its files leave the repository.

    This step cannot be automated: no AWS CLI is configured on this machine, so Amplify state can be neither inspected nor changed from here. The executor performs no file changes in this task. Nothing in the repository has been touched yet — `amplify.yml` and `deploy.sh` are both still present, so Amplify remains a working emergency fallback for the entire duration of this checkpoint. That is deliberate: the fallback exists precisely to cover this transition, and it is removed only in Task 3, after this checkpoint is confirmed.

    Present the instructions below to the user verbatim and halt until they respond.
  </action>
  <instructions>
    Click-path quoted from AWS's own current documentation:

    1. Sign in to the AWS Amplify console at `console.aws.amazon.com/amplify`.
    2. Select the app serving this site.
    3. In the left nav, choose **App settings** -> **Branch settings**.
    4. In the **Branches** table, locate the `main` branch row.
    5. Click that row's **Actions** menu -> **Disable auto build**.

    This does NOT delete the app, disconnect the GitHub integration, or remove the branch. It only stops the webhook-triggered auto-build. The app and its last successful deploy stay intact, so rollback remains available: the same menu offers **Actions** -> **Enable auto build** and restores the old behaviour in under a minute.

    Then observe before approving:

    6. Confirm `qiankun.co.uk` still loads correctly in a browser.
    7. Confirm in the Amplify console's build history that no new Amplify build has fired.

    The length of the observation window is your call — a few minutes is sufficient if the site is loading and no build has triggered; take up to a day if you would rather be certain. Nothing in the repo changes while you wait.
  </instructions>
  <verification>
    Confirmation is the user's report on three points: auto build shows as disabled on the `main` branch row, `qiankun.co.uk` still serves correctly, and no new Amplify build appears in the build history during the observation window. There is no repository-side evidence for any of these — this is a `verification: backstop` truth, so the recorded human confirmation is the only evidence that exists. If the user cannot confirm all three, do not proceed to Task 3; report the truth as unmet.
  </verification>
  <acceptance_criteria>
    - The user explicitly confirms auto build is disabled on the `main` branch in the Amplify console
    - The user explicitly confirms `qiankun.co.uk` is still serving correctly
    - The user explicitly confirms no new Amplify build fired during the observation window
    - This confirmation is recorded in the SUMMARY as the INFRA-02 evidence (it is a `verification: backstop` truth — it cannot be checked from the repository, so the recorded human confirmation is the only evidence; do not mark it satisfied without one)
  </acceptance_criteria>
  <resume-signal>Type "approved" once auto build is disabled and the site is observed stable, or describe what you saw instead.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Delete the Amplify pipeline files and correct the README deploy path</name>
  <files>amplify.yml, deploy.sh, package.json, README.md</files>
  <read_first>
    - `amplify.yml` (18-line Amplify build spec being deleted)
    - `deploy.sh` (bash script that commits, pushes, then polls `aws amplify list-jobs`; assumes Amplify is the live target)
    - `package.json` lines 6-12 — the `scripts` block; `deploy` and `deploy:quick` both invoke `./deploy.sh`
    - `README.md` lines 23-29 — the Deploy section, which currently instructs `npm run deploy` and states the site deploys to AWS Amplify
    - `.github/workflows/deploy.yml` (the surviving pipeline the README must describe instead: push to `main` -> checkout -> Node 22 -> `npm ci` -> `npm run docs:build` -> `npx wrangler pages deploy docs/.vitepress/dist --project-name=qiankun-website`)
  </read_first>
  <action>
    Only run this task after Task 2 is confirmed — the ordering is a safety property, not a preference.

    Delete `amplify.yml` and `deploy.sh` from the repository. In `package.json`, remove both the `deploy` and `deploy:quick` entries from the `scripts` object, leaving `docs:dev`, `docs:build`, and `docs:preview`; fix the trailing comma so the JSON stays valid after removing the last two entries. These script removals must land in the same commit as the `deploy.sh` deletion — leaving them behind creates entry points that invoke a script that no longer exists.

    Rewrite the README Deploy section so it describes the surviving pipeline accurately: deployment is triggered by pushing to `main`, which runs the GitHub Actions workflow that builds and publishes to Cloudflare Pages. Remove the `npm run deploy` instruction and the sentence naming AWS Amplify. Do not leave a note explaining what used to be there — delete the stale content rather than annotating it, and do not name the removed provider anywhere in the file. Leave the README's Plugins section alone in this plan; plan 01-02 corrects it as part of the dependency change.

    Do not touch `.github/workflows/deploy.yml` in this plan — plan 01-02 edits it, and splitting the edits keeps the two commits independently revertable.
  </action>
  <verify>
    <automated>test ! -e amplify.yml &amp;&amp; test ! -e deploy.sh &amp;&amp; node -e "const s=require('./package.json').scripts; process.exit((s.deploy||s['deploy:quick'])?1:0)" &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `test -e amplify.yml` exits non-zero (file gone)
    - `test -e deploy.sh` exits non-zero (file gone)
    - `node -p "Object.keys(require('./package.json').scripts).join(',')"` prints exactly `docs:dev,docs:build,docs:preview`
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (package.json is still valid JSON after the comma fix)
    - `grep -ci 'amplify' README.md` returns `0`
    - `grep -c 'npm run deploy' README.md` returns `0`
    - `grep -ci 'cloudflare' README.md` returns at least `1`
    - `git show --stat HEAD` shows `deploy.sh` and `package.json` changed in the same commit
  </acceptance_criteria>
  <reversibility rating="reversible">`git revert` restores both files and the two scripts; Amplify auto-build re-enables from the same console menu in under a minute. Both halves of the rollback are independently available.</reversibility>
  <done>Exactly one deploy pipeline exists in the repository, no dangling script invokes a deleted file, and the README documents the real deploy path (INFRA-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub `main` -> live site | A push to `main` can publish to a domain printed on a CV. Currently two independent systems watch this branch. |
| AWS console -> deploy capability | Amplify's auto-build is controlled outside the repo; repo state alone cannot prove or change it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Dual deploy pipelines (Amplify auto-build + GitHub Actions) both able to publish to `qiankun.co.uk` | medium | mitigate | Task 2 disables Amplify auto-build; Task 3 removes `amplify.yml`/`deploy.sh`/`scripts.deploy` so only the GitHub Actions path remains |
| T-01-02 | Denial of Service | Deleting `amplify.yml` and `deploy.sh` before Amplify is confirmed disabled removes the only emergency fallback if Cloudflare fails mid-transition | high | mitigate | Task 3 is hard-gated behind the Task 2 blocking human checkpoint plus an observation window; the repo is untouched for the entire checkpoint duration |
| T-01-03 | Information Disclosure | `README.md` documents a deploy path that no longer exists, misleading anyone (including the hiring audience browsing the repo) about how the site ships | low | mitigate | Task 3 rewrites the Deploy section to describe the actual GitHub Actions to Cloudflare Pages path |
| T-01-04 | Tampering | DNS records altered during verification, breaking an established domain already printed on published materials | high | mitigate | Task 1 is explicitly read-only — query only, and the action forbids any record modification |
</threat_model>

<verification>
- `dig` output for both apex and `www` recorded verbatim in the SUMMARY, showing Cloudflare ranges and no AWS endpoint
- Human confirmation of Amplify auto-build disabled + site stable recorded in the SUMMARY (backstop truth — no repo evidence exists for this)
- `amplify.yml` and `deploy.sh` absent from the working tree and from `git ls-files`
- `package.json` parses as valid JSON and exposes only the three `docs:*` scripts
- `README.md` contains no reference to the decommissioned provider and names Cloudflare
</verification>

<success_criteria>
INFRA-01, INFRA-02, and INFRA-03 are satisfied: the serving pipeline was observed directly before any change, Amplify auto-deploy was disabled and the site observed stable before repo files were deleted, and exactly one pipeline can now trigger a deploy.
</success_criteria>

<output>
Create `.planning/phases/01-technical-foundations/01-01-SUMMARY.md` when done. It MUST include the verbatim `dig` output (INFRA-01 evidence) and the human's Amplify confirmation (INFRA-02 backstop evidence).
</output>
