---
phase: 01-technical-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - docs/.vitepress/config.js
  - .github/workflows/deploy.yml
  - README.md
autonomous: true
requirements: [INFRA-04, INFRA-05, INFRA-06, INFRA-07, INFRA-08]

must_haves:
  truths:
    - "`node -p \"require('./package.json').devDependencies.vitepress\"` prints exactly `2.0.0-alpha.18` with no caret"
    - "`node -p \"require('./package.json').devDependencies['vite-plugin-pwa']\"` prints `^1.3.0`"
    - "no dependency whose name contains `mermaid` appears in `package.json`"
    - "`docs/.vitepress/config.js` exports a bare `defineConfig({...})` with no wrapper call and no mermaid config key"
    - "`npm ci` with no flags exits 0 and emits no ERESOLVE output on a clean `node_modules`"
    - "`npm run docs:build` exits 0"
    - "no file outside `.planning/` and `node_modules/` contains the legacy peer-deps install flag"
  artifacts:
    - "package-lock.json regenerated from a clean tree and committed"
  key_links:
    - "The `package.json` mermaid removal and the `config.js` unwrap must land together — removing the dependency while the config still imports it breaks the build outright."
    - "The lockfile must be regenerated in the same commit as the `package.json` dependency edits, or CI's `npm ci` fails on a lockfile that disagrees with the manifest."
---

<!-- planner-discipline-allow: legacy-peer-deps -->
<!-- planner-discipline-allow: withMermaid -->
<!-- planner-discipline-allow: vitepress-plugin-mermaid -->
<!-- planner-discipline-allow: mermaid -->

<objective>
Make `npm install` resolve cleanly with no suppression flag, on correctly-pinned versions, and prove it with a real clean install and a real local build.

Purpose: The install flag currently in CI masks two genuine peer conflicts. Removing the flag without fixing the conflicts breaks CI; fixing the conflicts without removing the flag leaves the credibility problem in place. Both halves ship together, gated on a local build.
Output: Three dependency changes, a mermaid-free VitePress config, a regenerated lockfile, a flag-free CI workflow, and a corrected README plugin list.
</objective>

<execution_context>
@/Users/qiankun/.claude/gsd-core/workflows/execute-plan.md
@/Users/qiankun/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-technical-foundations/01-CONTEXT.md
@.planning/phases/01-technical-foundations/01-RESEARCH.md
@.planning/phases/01-technical-foundations/01-PATTERNS.md
@.planning/phases/01-technical-foundations/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

Modified by this plan:
- `package.json` — `vite-plugin-pwa` bumped, `vitepress` pinned exact, two mermaid packages removed
- `package-lock.json` — regenerated from a clean tree
- `docs/.vitepress/config.js` — plugin wrapper unwrapped to a bare `defineConfig`, mermaid config key removed
- `.github/workflows/deploy.yml` — install flag removed
- `README.md` — Plugins list corrected

No new files. No new packages: `vite-plugin-pwa` is an existing dependency receiving a version bump, and the two mermaid packages are deletions. RESEARCH.md's Package Legitimacy Audit records zero `[ASSUMED]`, `[SUS]`, or `[SLOP]` entries for this phase, so no legitimacy checkpoint is required before install.

<tasks>

<task type="tracer">
  <name>Task 1: Fix the dependency set end-to-end and prove it with a clean install and local build</name>
  <precondition>Network access to the public npm registry is available — the lockfile must be regenerated against the live registry, so an offline or proxied environment will produce a wrong result. Halt and report if `npm ping` fails.</precondition>
  <files>package.json, package-lock.json, docs/.vitepress/config.js</files>
  <read_first>
    - `package.json` lines 18-31 — the `devDependencies` block being edited
    - `docs/.vitepress/config.js` line 4 (the plugin import), line 29 (the wrapper call opening `export default`), lines 87-90 (the mermaid config key and its comment), and lines 253-254 (the file's closing `}),` + `);` which must collapse to `});`)
    - `.planning/phases/01-technical-foundations/01-PATTERNS.md` section `docs/.vitepress/config.js (unwrap withMermaid, remove GA)` — has the exact verified before/after shape for the unwrap
    - `.planning/phases/01-technical-foundations/01-RESEARCH.md` section `Verified Dependency Resolution (empirical, this repo)` — the exact post-fix dependency set was already run through `npm install --dry-run` and resolved with zero conflicts; and section `Pitfall 4` on the expected incidental `vite` patch bump
  </read_first>
  <action>
    This is the thin end-to-end slice for the phase: manifest -> config -> lockfile -> real build. All of it lands or none of it does.

    In `package.json` `devDependencies`: change `vite-plugin-pwa` from `^1.2.0` to `^1.3.0` (per INFRA-05 — this version's peer range adds Vite 8, which clears the first conflict). Change `vitepress` from `^2.0.0-alpha.18` to the exact string `2.0.0-alpha.18` with no caret (per INFRA-07 — the caret would let a future alpha install silently). Delete the `mermaid` entry and the `vitepress-plugin-mermaid` entry outright (per INFRA-06 — the plugin is dead upstream with no VitePress 2.x-compatible version, which is the actual structural reason the install flag exists). Do NOT downgrade VitePress to 1.6.4; that was considered and explicitly declined.

    In `docs/.vitepress/config.js`, perform the mechanical unwrap: delete the `withMermaid` import line; change the export so it opens directly as `export default defineConfig({` instead of wrapping that call; remove the now-orphaned closing paren at the end of the file so the file terminates with the `defineConfig` object's own close; and delete the `mermaid` config key together with its preceding comment line. Leave every other config key untouched — the `head` array is edited by plan 01-03, not here. Do not leave any comment naming the removed plugin: the acceptance gate greps this file case-insensitively for the substring and a leftover comment would fail a build that is otherwise correct.

    Regenerate the lockfile honestly rather than hand-editing it: remove `node_modules` and `package-lock.json`, run a fresh `npm install` with no flags, then remove `node_modules` again and run `npm ci` with no flags to prove the regenerated lockfile is self-consistent. Expect the lockfile to pick up `vite` at a patch above the `^8.1.4` currently declared — that is correct caret-range behaviour, not an unintended change, and needs no manifest edit; note it in the commit message so a reviewer does not misread it.

    Finally run the production build locally. The build passing is a precondition of committing, not a follow-up check — the user granted autonomy on deploy-affecting changes on exactly this condition. If the build fails, the most likely cause is a bracket or paren mismatch from the unwrap; fix and re-run before committing anything.

    Known and accepted side effect: `docs/blog/vite-plugins.md` contains a diagram fence whose renderer is being removed, so that one diagram degrades to a plain code block. This does not break the build (an unrecognised fence renders as a code block). Do NOT delete that post and do NOT try to preserve the diagram — the post is removed by PRUNE-04 in phase 2, so the degraded state lasts one phase on a page already slated for deletion.
  </action>
  <verify>
    <automated>rm -rf node_modules &amp;&amp; npm ci &amp;&amp; npm run docs:build</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').devDependencies.vitepress"` prints exactly `2.0.0-alpha.18`
    - `node -p "require('./package.json').devDependencies['vite-plugin-pwa']"` prints exactly `^1.3.0`
    - `grep -ci 'mermaid' package.json` returns `0`
    - `grep -ci 'mermaid' docs/.vitepress/config.js` returns `0`
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0
    - `npm ci` on a removed `node_modules` exits 0
    - `npm ci 2>&1 | grep -ci 'ERESOLVE'` returns `0`
    - `npm run docs:build` exits 0
    - `test -f docs/.vitepress/dist/index.html` exits 0 (build actually emitted output)
    - `git status --porcelain package-lock.json` shows the lockfile as modified (it was regenerated, not left stale)
  </acceptance_criteria>
  <reversibility rating="reversible">`git revert` restores the manifest and the previous known-good lockfile together. The local build gate means a broken build never reaches `main` in the first place.</reversibility>
  <done>A clean checkout installs with no flag and no peer conflict, and the site builds (INFRA-05, INFRA-06, INFRA-07, INFRA-08).</done>
</task>

<task type="auto">
  <name>Task 2: Remove the install flag from CI and correct the stale README plugin list</name>
  <files>.github/workflows/deploy.yml, README.md</files>
  <read_first>
    - `.github/workflows/deploy.yml` line 21 — the install step carrying the flag
    - `README.md` lines 46-54 — the Plugins list, which still advertises the removed plugin
    - `.planning/phases/01-technical-foundations/01-RESEARCH.md` section `Pitfall 3: Stale documentation surviving the dependency cleanup` — explains why a grep for the flag string alone does not catch the README
  </read_first>
  <action>
    In `.github/workflows/deploy.yml`, change the install step so it runs `npm ci` with no additional arguments. Task 1 already proved this resolves cleanly, so the flag has nothing left to suppress. Change nothing else in the workflow.

    In `README.md`, remove the bullet advertising the mermaid VitePress plugin from the Plugins list, since that package no longer exists in the manifest. Leave the remaining plugin bullets as they are. The Deploy section was already corrected by plan 01-01 — do not re-edit it.

    Deliberately NOT in scope: RESEARCH.md flags the hardcoded Cloudflare account ID in this workflow as a secrets-hygiene nit and suggests moving it to a GitHub Actions secret. That is not covered by any of INFRA-01..10, an account ID is not a credential, and making the change would introduce a dependency on a repository secret that does not exist yet — a missing secret would break every deploy. Leave it; carry it as a backlog note in the SUMMARY instead.

    INFRA-04 says the flag must be absent from CI, deploy scripts, AND documentation. The deploy scripts were deleted in plan 01-01, this task covers CI and documentation. The acceptance gate greps the whole working tree excluding `node_modules`, `.git`, and `.planning` — the planning corpus legitimately discusses the flag by name and must stay excluded, or the gate can never pass.
  </action>
  <verify>
    <automated>test "$(grep -rIl 'legacy-peer-deps' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning . | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rIl 'legacy-peer-deps' --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning . | wc -l` returns `0`
    - `grep -cx '      - run: npm ci' .github/workflows/deploy.yml` returns `1` — whole-line match with the original six-space indent, so a whitespace-mangled edit fails this gate (do not substitute a YAML parser here; this repo has no YAML library installed for either node or python3)
    - `grep -c 'npm ci' .github/workflows/deploy.yml` returns `1` (no second install step was introduced)
    - `grep -ci 'mermaid' README.md` returns `0`
    - `grep -c 'vitepress-plugin-rss' README.md` returns at least `1` (the other plugin bullets survived)
  </acceptance_criteria>
  <reversibility rating="reversible">Single-line CI edit and a README bullet; `git revert` restores both.</reversibility>
  <done>No suppression flag remains anywhere in CI, scripts, or documentation, and the README describes the real plugin set (INFRA-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build environment | Lockfile regeneration pulls package content from a public registry into both local and CI builds. |
| repo `main` -> CI build -> live site | A manifest or config change that breaks the build reaches the deploy pipeline on push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (`npm install` / `npm ci` during lockfile regeneration) | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` was completed for this phase: no new package names are introduced, the only version bump is `vite-plugin-pwa@1.3.0` from the established `vite-pwa` org, and zero packages are flagged `[ASSUMED]`, `[SUS]`, or `[SLOP]` — so no blocking legitimacy checkpoint is required. Review the lockfile diff for unexpected major-version movement before committing. |
| T-01-05 | Denial of Service | A broken build (most plausibly a paren mismatch from the config unwrap) reaching `main` and failing CI or publishing a broken site | high | mitigate | Task 1 makes a passing local `npm ci` + `npm run docs:build` a hard precondition of committing, not a follow-up check |
| T-01-06 | Tampering | Lockfile regeneration silently pulling transitive updates beyond the three intended changes | medium | mitigate | Regenerate from a clean tree, then re-verify with `npm ci`; the expected incidental `vite` patch bump is documented in advance so it is not mistaken for drift, and anything beyond it is reviewable in the lockfile diff |
| T-01-07 | Information Disclosure | Cloudflare account ID hardcoded in the public workflow file | low | accept | An account ID is not a credential; moving it to a secret introduces a deploy-breaking dependency on a secret that does not yet exist, for zero requirement coverage. Recorded as a backlog note rather than changed mid-phase. |
</threat_model>

<verification>
- A clean `npm ci` with no flags succeeds and emits no ERESOLVE output
- `npm run docs:build` succeeds and emits `docs/.vitepress/dist/index.html`
- `package.json` pins VitePress exactly, declares `vite-plugin-pwa` at `^1.3.0`, and contains no mermaid package
- `docs/.vitepress/config.js` contains no mermaid reference in any form and still parses (proven by the build)
- The suppression flag appears nowhere in the tree outside `.planning/` and `node_modules/`
</verification>

<success_criteria>
INFRA-04 through INFRA-08 are satisfied: dependencies resolve clean with no suppression flag anywhere in CI, scripts, or docs; `vite-plugin-pwa` is at `^1.3.0`; both mermaid packages are gone; VitePress is pinned exactly; and the site builds from a clean install.
</success_criteria>

<output>
Create `.planning/phases/01-technical-foundations/01-02-SUMMARY.md` when done. Record the `npm ci` and `npm run docs:build` results, the lockfile's incidental `vite` patch movement, and the deferred Cloudflare-account-ID hygiene note.
</output>
